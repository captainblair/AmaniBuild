import { ModulePlaceholder } from "@/components/dashboard/ModulePlaceholder";

export const metadata = { title: "Inspections" };

export default function InspectionsPlaceholderPage() {
  return (
    <ModulePlaceholder
      title="Inspections"
      description="QA inspections and issue reporting land in a later phase."
    />
  );
}
