import { ModulePlaceholder } from "@/components/dashboard/ModulePlaceholder";

export const metadata = { title: "Equipment" };

export default function EquipmentPlaceholderPage() {
  return (
    <ModulePlaceholder
      title="Equipment"
      description="Equipment tracking lands in a later phase."
    />
  );
}
